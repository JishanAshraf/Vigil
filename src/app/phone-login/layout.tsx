import React from 'react';

// This file is intentionally left blank as the phone authentication system has been removed.
export default function PhoneLoginLayout({
    children,
}: {
    children: React.ReactNode;
}) {
    return <>{children}</>;
}
